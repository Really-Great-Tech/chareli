name: Deploy to Dev Environment

on:
  push:
    branches:
      - staging
  pull_request:
    branches:
      - staging
  workflow_dispatch:

permissions:
  packages: write
  contents: read
  id-token: write

env:
  AWS_REGION: eu-central-1
  ECR_REGISTRY: 330858616968.dkr.ecr.eu-central-1.amazonaws.com
  ECR_REPO: chareli-dev-repo
  S3_BUCKET: chareli-react-app-dev-ea15b364
  # CLOUDFRONT_DISTRIBUTION_ID:

jobs:
  #   build-test-frontend:
  #     name: Build and Deploy Frontend
  #     runs-on: ubuntu-latest
  #     defaults:
  #       run:
  #         working-directory: ./client

  #     steps:
  #       - name: Checkout Code
  #         uses: actions/checkout@v4
  #         with:
  #           fetch-depth: 0

  #       - name: Setup Node.js
  #         uses: actions/setup-node@v4
  #         with:
  #           node-version: 22.11.0
  #           cache: "npm"

  #       - name: Install Dependencies
  #         run: npm ci

  #       # - name: Run lint and Unit tests
  #       #   run: pnpm lint && pnpm test

  #       - name: Build Frontend
  #         run: pnpm build
  #         env:
  #           VITE_API_URL:

  #       - name: Configure AWS credentials
  #         uses: aws-actions/configure-aws-credentials@v4
  #         with:
  #           role-to-assume: arn:aws:iam::330858616968:role/rgt-tickr-gha-oidc-role
  #           aws-region: ${{ env.AWS_REGION }}

  # Optional: Build and push Docker image for potential local testing
  # - name: Set up Docker BuildX
  #   id: buildx
  #   uses: docker/setup-buildx-action@v3

  # - name: Login to Amazon ECR
  #   id: login-ecr
  #   uses: aws-actions/amazon-ecr-login@v2

  # - name: Build and Push Frontend Docker Image to ECR (for reference)
  #   uses: docker/build-push-action@v6
  #   with:
  #     context: ./client
  #     file: ./client/Dockerfile
  #     push: true
  #     builder: ${{ steps.buildx.outputs.name }}
  #     tags: ${{ env.ECR_REGISTRY }}/${{ env.ECR_REPO }}:client-dev

  # Deploy to S3
  # - name: Deploy to S3
  #   run: |
  #     aws s3 sync dist/ s3://${{ env.S3_BUCKET }} --delete

  # # Invalidate CloudFront Cache
  # - name: Invalidate CloudFront
  #   run: |
  #     aws cloudfront create-invalidation --distribution-id ${{ env.CLOUDFRONT_DISTRIBUTION_ID }} --paths "/*"

  build-test-deploy-backend:
    name: Build and Deploy Backend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./Server

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.11.0
          cache: "npm"
          cache-dependency-path: Server/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Run lint and Unit tests
        run: pnpm lint && pnpm test
        continue-on-error: true

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::330858616968:role/rgt-tickr-gha-oidc-role
          aws-region: ${{ env.AWS_REGION }}

      - name: Set up Docker BuildX
        id: buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Generate Docker Image Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.ECR_REGISTRY }}/${{ env.ECR_REPO }}
          tags: |
            type=raw,value=server-dev

      - name: Build and Push Backend Docker Image to ECR
        uses: docker/build-push-action@v6
        with:
          context: ./Server
          file: ./Server/Dockerfile
          push: true
          builder: ${{ steps.buildx.outputs.name }}
          tags: ${{ steps.meta.outputs.tags }}

      # Deploy to EC2 using SSM Run Command
      # - name: Deploy to EC2
      #   run: |
      #     aws ssm send-command \
      #       --instance-ids "i-XXXXXXXXXXXX" \
      #       --document-name "AWS-RunShellScript" \
      #       --parameters commands=["cd /opt/tickr && sudo docker-compose pull && sudo docker-compose up -d"] \
      #       --comment "Deploying latest backend from CI/CD"
